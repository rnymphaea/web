import { DataService, Settings } from '../data/data.service';
import { SimulationGateway } from './simulation.gateway';
export declare class SimulationController {
    private readonly dataService;
    private readonly simulationGateway;
    constructor(dataService: DataService, simulationGateway: SimulationGateway);
    getSettings(): Settings;
    updateSettings(settings: Partial<Settings>): Settings;
    startSimulation(): {
        success: boolean;
    };
    stopSimulation(): {
        success: boolean;
    };
}
