import { DataService, Broker } from '../data/data.service';
export declare class BrokersController {
    private readonly dataService;
    constructor(dataService: DataService);
    getBrokers(): Broker[];
    createBroker(broker: Omit<Broker, 'id'>): Broker;
    updateBroker(id: string, updateData: Partial<Broker>): Broker;
    deleteBroker(id: string): {
        success: boolean;
    };
}
